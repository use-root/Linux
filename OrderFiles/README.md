# This practica it's about the basic command in linux.

For example:

We have files like this:

```zsh

app.js  index.html  style.css
image2.png  image.png  otherimage.jp
document.pdf  emails.word  paswd.pd

```

How can I organiza this files into directories ?

Well I create a script in bash to do this:

```zsh
Documents/  Images/  Web/
```

**The key is in each Directory you find each file and depend on his extencion like: .pdf, .word, .css, .img , etc**

With this practice, I'm learn about fucntions, maps, commands like: `awk, uniq -u` and the errors
